# Core Django
# NB: versions alignees sur l'environnement de production reellement deploye.
# Ne pas retrograder Django sous 5.2 : django-filter>=25 l'exige.
Django==5.2.4
djangorestframework==3.17.1
django-cors-headers==4.3.1
django-filter==25.2

# Database
psycopg2-binary==2.9.9

# Authentication
djangorestframework-simplejwt==5.3.1

# WebSockets
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0

# Celery
celery==5.3.4
redis==5.0.1

# Image processing
Pillow==10.1.0

# Environment variables
python-decouple==3.8

# API documentation
drf-yasg==1.21.15

# Timezone
pytz==2023.3

# Utilities
python-dateutil==2.8.2

# Importe par config/settings/development.py, qui est le module charge par
# le service gunicorn en production (config.wsgi). Doit donc etre installe.
django-extensions==3.2.3
